---
layout: none
---

{
  "posts" : [
    {% for post in site.posts %}{
    	"url" : "{{ post.url }}",
    	"title" : "{{ post.title }}",
   		"author" : "{{ post.author }}",
    	"date" : "{{ post.date }}"
    }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  	]
}